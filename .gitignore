# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
# But keep the Milkdown bundle dist (committed so deployment needs no Node.js)
!src/promptgrimoire/static/milkdown/dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Environment files (contain secrets)
.env
.env.local
.env.development
.env.production
.env.sandbox

# Keep the example template
!.env.example
.nicegui/

# Session logs (generated data)
logs/

# Claude Code local settings (machine-specific permissions)
.claude/settings.local.json

# Test output logs
test-failures.log
test-all.log

# E2E test screenshots (debug artifacts)
tests/e2e/screenshots/

# Temporary output directory (experiments, exports)
output/

# Editor backup files
*~

# LibreOffice lock files
.~lock.*#

# Git worktrees (parallel development)
.worktrees/
.playwright-mcp/
test-all-fixtures.log
luatex.*/
