# Investigation: Unbounded Memory Growth (#434)

Date: 2026-03-27 (updated 2026-03-28)
Investigator: Claude (Opus 4.6)
Status: Phase 4 complete, peer-reviewed — cleanup gap consistent with execution-order artefact
Branch: `memory-leak-434`

## Incident Summary

Production PromptGrimoire (NiceGUI 3.9.0, Python 3.14, single-process) exhibits
unbounded RSS growth over time. Two crashes in the telemetry window:

- **Epoch 1** (59caff8f): 12h57m uptime, ended by deploy (#433)
- **Epoch 2** (3e8a4e5a, #433 deployed): 13h22m uptime, SIGABRT at ~4.3 GB RSS
  (2026-03-27 12:12 AEDT / 01:12 UTC)

Bandaid deployed: PR #436 adds memory-threshold graceful restart at 3 GB with
manual reconnect button, exit code 75 for systemd restart.

## Telemetry Source

- Database: `output/incident/incident.db` (72.5 MB, 163,881 events across 5 sources + Beszel + GitHub)
- Tarball: `output/incident/telemetry-20260327-1249.tar.gz`
- Window: 2026-03-26 23:00 to 2026-03-27 13:00 AEDT (12:00 to 02:00 UTC)
- Diagnostic snapshots: 161 `memory_diagnostic` events over 13h22m (5-minute interval)

All timestamps below in UTC unless marked AEDT.

## Prior Work

**Synthetic memory probe** (previous session, not preserved in this worktree):
Tested bare NiceGUI element create/clear/delete cycles, closures capturing state,
and fire-and-forget tasks. All three showed flat memory (~2 MB growth over 50
cycles). **This weakens bare NiceGUI element leaks and simple task accumulation
as explanations.** The leak requires real NiceGUI client lifecycle (WebSocket
connect/disconnect). Evidence grade: **possible** (synthetic test only, production
path unconfirmed).

## Phase 1: Evidence Gathering

### Diagnostic Counter Trends

Query: `SELECT ts_utc, extra_json FROM jsonl_events WHERE event = 'memory_diagnostic' ORDER BY ts_utc`

Selected snapshots (every ~25 min). Column abbreviations map to
`memory_diagnostic` JSON keys: ws_reg = `app_ws_registry`, ws_pres_ws =
`app_ws_presence_workspaces`, ws_pres_cl = `app_ws_presence_clients`,
clients_total = `clients_total`, connected = `clients_connected`,
tasks = `asyncio_tasks_total`.

| Time (UTC) | RSS MB | clients_total | connected | gap | ws_reg | ws_pres_ws | ws_pres_cl | tasks |
|------------|--------|---------------|-----------|-----|--------|------------|------------|-------|
| 12:27 | 276 | 0 | 0 | 0 | 0 | 0 | 0 | 11 |
| 12:52 | 1143 | 74 | 71 | 3 | 61 | 61 | 64 | 406 |
| 13:17 | 1219 | 63 | 57 | 6 | 48 | 48 | 52 | 358 |
| 14:07 | 1278 | 53 | 50 | 3 | 42 | 42 | 46 | 322 |
| 15:47 | 1327 | 26 | 26 | 0 | 19 | 19 | 20 | 201 |
| 17:27 | 1404 | 18 | 18 | 0 | 11 | 11 | 11 | 158 |
| 19:07 | 1431 | 17 | 17 | 0 | 13 | 13 | 13 | 155 |
| 20:22 | 1450 | 78 | 30 | 48 | 21 | 21 | 22 | 266 |
| 22:27 | 2175 | 150 | 98 | 52 | 88 | 88 | 95 | 630 |
| 23:42 | 3388 | 165 | 124 | 41 | 116 | 117 | 137 | 874 |
| 00:58 | 4152 | 161 | 147 | 14 | 138 | 138 | 148 | 854 |
| 01:23* | 1330 | 188 | 24 | 164 | 40 | 44 | 76 | 780 |
| 01:48 | 2074 | 157 | 146 | 11 | 130 | 130 | 140 | 768 |

*01:23 is ~11 min after SIGABRT crash/restart at 01:12 UTC.

### Key Observations (facts)

1. **RSS is monotonically non-decreasing within each process lifetime.**
   At 15:47, 26 clients connected, RSS = 1327 MB. At 17:27, 18 clients, RSS = 1404 MB.
   At 19:07, 17 clients, RSS = 1431 MB. Load dropped but RSS never fell.

2. **Dict-level cleanup works.** `ws_reg`, `ws_pres_ws`, `ws_pres_cl` all track
   proportionally with `clients_connected`. When clients drop from 71 to 18, these
   counters drop correspondingly. The cleanup paths in `_handle_client_delete()`,
   `PersistenceManager.evict_workspace()`, and `AnnotationDocumentRegistry.remove()`
   execute correctly at the dict level.

3. **`clients_total` vs `clients_connected` gap is usually small (0-14)** during
   steady state. NiceGUI's `Client.delete()` removes from `Client.instances` after
   a 3-second reconnect timeout. The gap represents clients in the timeout window.

4. **Occasional large gaps during load spikes** (48 at 20:22, 52 at 22:27, 164 at
   01:23 post-crash). Under event loop saturation, the `asyncio.sleep(3.0)` in
   NiceGUI's `delete_content()` competes with page-render tasks. Cleanup is
   delayed when the event loop is busiest.

5. **`asyncio_tasks_total` tracks proportionally with connected clients**, not
   accumulating independently. At 17 clients: 155 tasks (~9/client). At 146
   clients: 768 tasks (~5/client). The ratio varies but is proportional, not
   growing independently.

6. **Post-crash restart at 01:23**: 1330 MB RSS in 11 minutes with reconnection
   storm (188 total clients, 24 still connected). This is 4.8x the cold-start
   baseline (276 MB), suggesting per-client allocations are substantial and the
   reconnection storm (many rapid connect/disconnect cycles) exacerbates retention.

### What the evidence does NOT tell us (inference boundaries)

- **Dict cleanup does not prove object GC.** `_workspace_presence.pop()` removes
  the dict reference to `_RemotePresence`, but if anything else references it
  (reference cycle, asyncio task closure, NiceGUI element tree), the Python
  objects persist in the heap. The counters going down tells us dicts are clean,
  not that memory is freed.

- **RSS growth cannot distinguish leak from fragmentation.** CPython's `pymalloc`
  and glibc `malloc` are known to cause severe RSS inflation in long-running,
  high-churn allocation patterns. 15x RSS growth (276 MB to 4.15 GB) is consistent
  with either a genuine leak, severe arena fragmentation, or both. A controlled
  E2E reproduction with `gc.collect()` is required to distinguish these.

- **The synthetic probe's negative result is production-path-unconfirmed.** It
  tested bare NiceGUI elements without real WebSocket lifecycle. The annotation
  page creates a PageState with 66 fields, CRDT documents (avg 15 KB binary
  state), element trees, and multiple closures. These are absent from the
  synthetic test.

## Cleanup Chain (code audit)

Verified by reading source, paths below are in `src/promptgrimoire/`:

### Disconnect path

1. Browser disconnects WebSocket
2. NiceGUI `Client.handle_disconnect()` starts 3s timer
3. If no reconnect: `Client.delete()` → calls `on_client_delete()` handlers
4. Our `on_client_delete()` (`pages/annotation/broadcast.py:251-300`):
   - `_workspace_presence[workspace_key].pop(client_id)` — removes dict entry
   - If last client: `del _workspace_presence[workspace_key]`
   - Persists CRDT state: `pm.force_persist_workspace(workspace_id)`
   - If last client: `pm.evict_workspace()` + `_workspace_registry.remove(doc_id)`

### What cleanup does NOT do

- **Does not null closures.** `handle_update_from_other()` and `handle_peer_left()`
  closures capture `state` (PageState). After `_workspace_presence.pop()`, if
  reference cycles exist, GC must collect them. No explicit nulling occurs.

- **Does not clear `_broadcast_callback`.** `AnnotationDocument.set_broadcast_callback(None)`
  is never called from application code. The callback is only released when the
  AnnotationDocument itself is GC'd (after `_workspace_registry.remove(doc_id)`
  drops the last registry reference). This is a secondary concern — the callback
  only leaks if the document leaks.

- **Does not force `gc.collect()`.** After cleanup, Python's generational GC
  handles reference cycles on its own schedule.

### PageState size (potential retention concern)

`PageState` (`pages/annotation/__init__.py:207-358`) is a large dataclass:
- 66 annotated fields including `crdt_doc` (AnnotationDocument with pycrdt Doc),
  multiple `ui.element` references, `document_chars` (list of chars from HTML),
  `card_snapshots` dict, `document_tabs` dict, closures (`refresh_annotations`,
  `broadcast_update`, etc.)
- If a single reference to PageState survives, the entire object graph is pinned.

### Closure reference chains (speculative)

```
handle_update_from_other (closure) → captures `state` (PageState)
    PageState.broadcast_update → captures `_workspace_presence` (global dict)
    PageState.crdt_doc → AnnotationDocument → _broadcast_callback (never nulled)
    PageState.annotations_container → NiceGUI element tree
```

If PageState → callback → closure → state forms a cycle, CPython's cycle collector
should handle it, but large object graphs can survive multiple GC generations.
This is **speculative** — needs measurement.

## Hypotheses

All hypotheses are **speculative** (evidence grade: very low). No positive or
negative border has been tested on the production code path.

| # | Hypothesis | Mechanism | Why plausible |
|---|-----------|-----------|---------------|
| H1 | Closure reference cycles pin PageState after disconnect | `handle_update_from_other` → `state` → `broadcast_update` → `_workspace_presence` | Code shows closures capture state; no explicit nulling on cleanup |
| H2 | NiceGUI element trees retained after `Client.delete()` | `remove_all_elements()` may not break all internal refs | NiceGUI #4521 (circular refs), #2589 (`_handle_delete` issues) — upstream, unresolved |
| H3 | pycrdt `Doc` objects retain C-level memory | Yrs FFI allocations not returned to Python allocator | CRDT docs avg 15 KB, 130+ loaded concurrently |
| H4 | PersistenceManager cancelled tasks retain closure refs | `task.cancel()` doesn't release closure captures | Cancelled tasks are Python objects until GC'd |
| H5 | Event-loop saturation delays cleanup | Under load, NiceGUI's `asyncio.sleep(3)` → `Client.delete()` can't fire promptly | Post-crash: 164 gap at 01:23; cleanup competes with renders |
| H6 | malloc arena fragmentation (not a leak) | CPython/glibc don't return freed pages to OS | Known behaviour in long-running Python; 15x growth is plausible for fragmentation alone |

**H6 is the null hypothesis.** If the E2E probe shows RSS stabilises after
`gc.collect()` + `malloc_trim()`, the growth is fragmentation, not a leak.

## Phase 3: Memory Probe Results

### Single-user probe (nicegui_user fixture)

Test: `tests/integration/test_memory_leak_probe.py::TestMemoryLeakProbe::test_memory_growth_across_cycles`

Uses `nicegui_user` fixture with the production-extracted heavy workspace
(`workspace_pabai_190hl_scrubbed.json`): 181 KB CRDT state, 190 highlights,
11 tags, 426 KB document. 10 connect/disconnect cycles, each cycle:

1. `nicegui_user.open("/annotation?workspace_id=<id>")` — full page render
2. Wait for `doc-container` test ID (confirms CRDT load + element tree build)
3. Explicit `Client.delete()` on old clients (exercises cleanup chain)
4. `nicegui_user.open("/login")` — navigate away
5. Measure RSS, `Client.instances`, `_workspace_registry`, `_workspace_presence`, `asyncio.all_tasks()`, `gc.get_objects()`
6. `gc.collect()` x 2, measure again
7. `malloc_trim()`, measure again

### Predictions (stated before run)

- If RSS grows linearly per cycle after `gc.collect()`: genuine leak (H1-H5)
- If RSS stabilises after `gc.collect()`: fragmentation (H6), no code fix needed

### Results (2026-03-28)

Full output: `output/incident/probe_results.txt`

| Metric | Cycle 1 | Cycles 2-10 | Trend |
|--------|---------|-------------|-------|
| RSS after_trim (MB) | 365 | 384-385 | **Flat** (±0 after cycle 2) |
| gc_objects after GC | 544,145 | 545,050-545,207 | **Flat** (+62 objects total over 9 cycles) |
| gc_collected per cycle | 100,793 | 167,711–168,530 | **Constant after warm-up** (cycle 1 lower; cycles 3+ identical) |
| Client.instances | 3 | 3 | **Constant** |
| ws_registry docs | 1 | 1 | **Constant** |
| ws_presence clients | 1 | 1 | **Constant** |
| asyncio tasks | 9 | 10 | **Constant** |

### Analysis

**Important caveats on this probe's cleanup path:** The `nicegui_user`
fixture does NOT trigger the real WebSocket disconnect → `Client.delete()`
path. The probe manually calls `Client.delete()` on old clients
(test_memory_leak_probe.py:368-372) to exercise the cleanup chain.
This means the probe tests whether cleanup **can** free memory, not
whether production cleanup **does** free memory. Results below are
conditional on forced cleanup + forced GC.

1. **Under forced cleanup + GC, RSS is flat.** RSS stabilises at 385 MB
   after `gc.collect()` + `malloc_trim()` across cycles 2-10. The
   prediction for H6 (fragmentation) is matched for this cleanup path.

2. **Dict-level counters reach zero; deeper object retention not measured.**
   `Client.instances`, `_workspace_registry`, `_workspace_presence`,
   `asyncio.all_tasks()` are the only counters checked. PageState instances,
   NiceGUI element trees, pycrdt Doc objects, and closures are **not
   individually counted**. The flat RSS suggests they are freed, but this
   is inference from aggregate RSS, not direct measurement.

3. **H1-H5 weakened but not ruled out.** With explicit `Client.delete()`
   and `gc.collect()`, the ~168K objects per cycle are collected and RSS
   does not grow. This weakens H1-H4 (the objects are **collectible**)
   but does not rule them out under production conditions where cleanup
   is natural, not forced, and GC runs on its own schedule.

4. **H5 (event-loop saturation delays cleanup) remains plausible as an
   amplifier.** The probe runs single-user with no event-loop contention.
   In production under load, `Client.delete()` may be delayed, and
   `gc.collect()` never runs explicitly. Collectible objects may persist
   longer than expected under load.

### Evidence grading update

| # | Hypothesis | Prior | Updated | Rationale |
|---|-----------|-------|---------|-----------|
| H1 | Closure reference cycles | Speculative | **Weakened** — possible | Collectible under forced cleanup; production conditions untested |
| H2 | NiceGUI element retention | Speculative | **Weakened** — possible | Released after forced Client.delete() + GC; upstream #5110 unresolved |
| H3 | pycrdt C-level retention | Speculative | **Weakened** — possible | RSS flat under forced cleanup; Rust-side allocations not directly measured |
| H4 | Cancelled task closures | Speculative | **Weakened** — possible | Task count constant at 10 under forced cleanup |
| H5 | Event-loop saturation | Speculative | **Plausible** | Not testable single-user; production telemetry shows delayed cleanup under load |
| H6 | malloc arena fragmentation | Speculative | **Plausible** | RSS flat after forced GC+trim; see E2E probe below which complicates this |

**H6 is plausible but not yet the confirmed explanation.** The single-user
probe shows that objects are collectible and RSS stabilises under forced
cleanup. But this probe bypasses the real WebSocket disconnect path and
forces cleanup that production does not perform. See E2E probe below.

### Limitations (epistemic boundary)

- **Single-user test.** Does not exercise concurrent access, WebSocket
  multiplexing, or event-loop contention. H5 cannot be tested here.
- **nicegui_user fixture vs real WebSocket.** The `nicegui_user` fixture
  simulates HTTP-level client interaction, not real WebSocket frames. Some
  NiceGUI internal paths may differ.
- **No interaction beyond page load.** The probe loads the page and
  disconnects. It does not exercise CRDT edits, tag operations, or
  highlight creation within a session.
- **Evidence grade: plausible, not demonstrated.** To reach "demonstrated"
  for H6, we would need to show the same flat-after-GC pattern under
  concurrent multi-user load, AND show that removing GC/trim causes growth.
  The negative border (RSS grows without GC) is inferred from production
  telemetry, not directly tested.

### E2E probe: 10 concurrent Playwright browsers (2026-03-28)

Test: `tests/e2e/test_memory_probe_434.py::TestMemoryProbe434::test_concurrent_memory_growth`

Real Playwright browsers (Chromium), 10 contexts per cycle, 5 cycles. Same
Pabai workspace (190 highlights, 181 KB CRDT state, 426 KB document content,
761 KB fixture file total). Each cycle: authenticate 10 users → all open
annotation page → wait for full render → close all browser contexts →
wait 3s for WebSocket disconnect → **`/api/test/cleanup`** (force-deletes
stale NiceGUI clients, disconnects orphan engine.io sessions, cancels
orphan `Event.wait` tasks) → `/api/test/gc` (gc.collect x 2 + malloc_trim)
→ measure via `/api/test/diagnostics`.

**Cleanup caveat:** The `/api/test/cleanup` endpoint force-deletes clients
and cancels orphan tasks that production relies on natural timeouts to clear.
This means the probe measures retention **after aggressive forced cleanup**,
not after production-like natural cleanup. The +9 MB/cycle residue is a
lower bound — production retention per cycle may be higher.

Full output: `output/incident/e2e_probe_results.txt`

| Metric | Baseline | Cycle 1 | Cycle 5 | Trend |
|--------|----------|---------|---------|-------|
| RSS after gc+trim (MB) | 283 | 486 | 531 | **+9 MB/cycle** |
| RSS with 10 connected (MB) | — | 574 | 598 | +6 MB/cycle |
| gc_collected | 256 | 726,567 | 756,696 | ~Constant |
| clients after GC | 0 | 0 | 0 | Clean |
| presence_clients after GC | 0 | 0 | 0 | Clean |
| ws_registry after GC | 0 | 0 | 0 | Clean |
| asyncio_tasks | 12 | 13 | 13 | Constant |

#### Analysis

1. **Small but measurable RSS growth: ~9 MB per cycle of 10 clients.**
   RSS after gc+trim: 486 → 506 → 510 → 517 → 531 MB. Growth ratio
   over 5 cycles: 1.09x. Well under the 1.5x alarm threshold, but
   **not flat** like the single-user NiceGUI probe showed.

2. **Five application-level counters reach zero after forced cleanup.**
   `nicegui_clients`, `presence_total_clients`, `ws_registry`,
   `asyncio_tasks` all drop to 0/baseline after `/api/test/cleanup` + GC.
   However, **these are the only counters measured**. PageState instances,
   NiceGUI element trees, pycrdt Doc objects, closure references, and
   socketio session data are not individually tracked. The retained
   ~9 MB/cycle may include un-measured Python objects, not just
   allocator fragmentation.

3. **~0.9 MB per client retained after forced GC + malloc_trim.** This is
   memory that survives aggressive forced cleanup — candidates:
   - glibc arena internal fragmentation (small allocations between large
     ones prevent page release even after malloc_trim; `malloc_trim(0)`
     cannot reclaim partially-filled thread-local arenas)
   - pycrdt Yrs FFI allocations (Rust-side memory managed by a separate
     allocator invisible to Python GC and glibc malloc_trim)
   - NiceGUI internal structures not covered by `Client.delete()`
     (known circular reference issue #5110 in element slot children)
   - python-socketio per-session state (request objects, event handlers)

   (RSS is measured server-side via `/proc/self/status`, so browser-side
   memory is not a factor.)

4. **Production extrapolation (speculative).** At ~0.9 MB/client retained
   under forced cleanup, production retention is likely higher because:
   - Production has no forced cleanup or explicit GC
   - Event loop saturation delays natural cleanup (H5)
   - The probe does not exercise CRDT edits, tag operations, or highlight
     creation — only page load and disconnect
   A rough estimate of 200 clients/day x ≥0.9 MB ≈ ≥180 MB/day is
   plausible but imprecise.

5. **The #436 memory-threshold restart is the correct operational
   mitigation.** Whether the residue is allocator fragmentation,
   un-measured Python object retention, or both, the threshold restart
   at 3 GB keeps the process healthy. Further investigation to pinpoint
   the exact allocator would require C-level profiling (jemalloc swap,
   `MALLOC_ARENA_MAX` tuning, `/proc/[pid]/smaps` analysis) — see
   Recommendations below.

### Natural cleanup probe (2026-03-28)

Test: `test_memory_probe_434.py::TestMemoryProbe434::test_natural_cleanup_memory_growth`

Same as the forced-cleanup E2E probe but **without `/api/test/cleanup`**.
After browser contexts close, the probe waits 5s for NiceGUI's natural
reconnect_timeout (0.5s in E2E) → `Client.delete()` → `on_client_delete()`
chain to complete, then runs `/api/test/gc` only.

This answers the reviewer question: "does the natural cleanup path actually
free memory, or does forced cleanup mask retention?"

Full output: `output/incident/e2e_probe_natural.txt` (natural),
`output/incident/e2e_probe_forced.txt` (forced, same run).

#### Side-by-side comparison

| Metric | Forced cleanup | Natural cleanup |
|--------|---------------|-----------------|
| Baseline RSS (MB) | 517* | 282 |
| Cycle 1 after GC (MB) | 518 | 488 |
| Cycle 5 after GC (MB) | 523 | 517 |
| Growth over 5 cycles | +5 MB (1.01x) | +29 MB (1.06x) |
| Per-cycle growth | ~1 MB/cycle | ~6 MB/cycle |
| Presence after GC | 0 | 0 |
| Clients after GC | 0 | 0 |

*Execution order inferred from baseline RSS values: natural (282 MB
baseline, consistent with cold server) ran before forced (517 MB baseline,
consistent with warm server after natural's 5 cycles). The `test-perf.log`
referenced in prior drafts is not preserved in this worktree. The absolute
RSS values are not directly comparable; the per-cycle growth rates are.

#### Analysis

1. **Natural cleanup works.** All 5 tracked counters (clients, presence,
   registry, tasks) reach zero without forced cleanup. NiceGUI's
   reconnect_timeout → `Client.delete()` → `on_client_delete()` chain fires
   correctly for all 10 clients within 5s.

2. **Natural cleanup retains ~6 MB/cycle vs forced cleanup's ~1 MB/cycle.**
   The ~5 MB/cycle difference (~0.5 MB/client) represents memory that
   `/api/test/cleanup`'s extra steps reclaim. That endpoint does three
   things beyond natural cleanup: force-delete clients, disconnect orphan
   engine.io sessions, and cancel orphan `Event.wait` tasks. Which of
   these three accounts for the gap is not yet isolated.

3. **Both variants show the same pattern:** tracked counters reach zero,
   but RSS grows. The retained memory is below the 5-counter measurement
   surface in both cases.

4. **The natural cleanup result partially validates H5.** Even with a fast
   0.5s reconnect_timeout (vs 30s in production), natural cleanup leaves
   ~5x more residue per cycle than forced cleanup. In production with 30s
   timeout and event-loop contention, the gap is likely larger.

#### Updated evidence grading

| # | Hypothesis | Grade | Rationale |
|---|-----------|-------|-----------|
| H1-H4 | Application-level leaks | **Weakened** — possible | Tracked counters zero under both forced and natural cleanup. Deeper objects not measured. Objects are collectible but ~0.5 MB/client retained under natural conditions that forced cleanup can reclaim |
| H5 | Cleanup path leaves residue | **Plausible** (strengthened) | Natural cleanup retains ~6 MB/cycle vs forced ~1 MB/cycle. The ~5 MB gap is memory that natural cleanup doesn't reach. Production (30s timeout, event-loop load) likely retains more |
| H6 | glibc arena fragmentation | **Plausible** | ~1 MB/cycle residue survives even forced cleanup + GC + trim. Consistent with arena fragmentation that no application-level action can reclaim |
| H7 | Sub-allocator retention | **Possible** | The ~1 MB/cycle forced-cleanup residue could be pycrdt Yrs FFI, NiceGUI #5110 circular refs, or glibc internal fragmentation. Cannot distinguish without allocator-level profiling |

#### Epistemic boundary

- **Plausible:** Natural cleanup leaves ~6 MB/cycle residue; forced cleanup
  leaves ~1 MB/cycle. The ~5 MB gap is attributable to the extra teardown
  in `/api/test/cleanup` (H5) but which of its three actions matters is
  not isolated. The remaining ~1 MB/cycle survives even aggressive forced
  cleanup (H6/H7).
- **Possible:** The ~5 MB gap could also include un-measured Python objects
  (PageState, element trees, closures) that forced cleanup's extra teardown
  steps happen to break references for. NiceGUI #5110 (circular refs in
  slot children) is a known upstream issue.
- **Not tested:** Which sub-allocator holds the ~1 MB/cycle forced-cleanup
  residue. Would require `MALLOC_ARENA_MAX` tuning, jemalloc swap, or
  `/proc/[pid]/smaps` analysis.
- **Practical conclusion:** The #436 threshold restart is the correct
  operational mitigation. Natural cleanup works (counters reach zero) but
  leaves ~0.6 MB/client/cycle that accumulates over a process lifetime.
  At 200 clients/day, that's ~120 MB/day minimum — hitting the 3 GB
  threshold in ~23 days under ideal conditions, faster under load.

## Peer Review (Phase 3d Self-Audit)

### Round 1: Phase 1 summary (2026-03-27)

Applied critical-peer-review to the initial Phase 1 summary. Three High findings:

1. **"Not just fragmentation" was unsupported.** Corrected: cannot distinguish
   leak from fragmentation without controlled measurement.
2. **"It's not our tracked objects" was inference dressed as fact.** Corrected:
   dict-level cleanup demonstrated; object-level GC unknown.
3. **H4 prematurely downgraded.** Corrected: all hypotheses remain speculative.

Two Medium findings:
1. Post-crash data point (01:23) reveals event-loop-saturation cleanup delay — added as H5.
2. H5 (asyncio task accumulation) weakened by proportional tracking — tasks vary 5-9/client.

### Round 2: Phase 3 probe results (2026-03-28)

Two independent reviews (proleptic challenger + code reviewer). Key findings
and corrections:

**HIGH findings (all corrected above):**

1. **"No genuine memory leak detected" overclaimed.** Both probes bypass the
   real WebSocket disconnect path — the single-user probe manually calls
   `Client.delete()`, the E2E probe calls `/api/test/cleanup` which
   force-deletes clients and cancels orphan tasks. Corrected: "Under forced
   cleanup + GC, tracked counters reach zero and RSS stabilises/grows slowly."

2. **"H1-H4 not supported" / "no Python objects retained" overclaimed.** The
   diagnostics only measure 5 counters (nicegui_clients, presence, registry,
   tasks, RSS). PageState, element trees, pycrdt Docs, closures, socketio
   session data are not individually tracked. Corrected: "H1-H4 weakened
   (objects are collectible) but not ruled out (deeper objects not measured)."

3. **PageState has 66 fields, not ~40.** Verified by AST count of
   `pages/annotation/__init__.py:207-358`. Corrected throughout.

4. **gc_collected "Constant" misstated.** Cycle 1 = 100,793; cycle 2 =
   167,711; cycles 3+ = 168,530. Corrected to "constant after warm-up."

5. **Telemetry table column names abbreviated without mapping.** Added
   explicit abbreviation → JSON key mapping.

**MEDIUM findings (all corrected above):**

1. **"Chromium-side residue" impossible.** RSS measured server-side via
   `/proc/self/status`. Removed from candidate list.

2. **Prior failing probe runs not reconciled.** Run at 2026-03-27T15:54
   failed with `ws_presence_clients=10`, `client_instances=31` after 10
   cycles — cleanup was not firing. The explicit `Client.delete()` loop
   was added after this failure. The passing run strongly suggests the
   loop is effective; the failing run strongly suggests it was needed.
   (The exact diff between runs was the addition of the delete loop at
   test_memory_leak_probe.py:368-372, but other factors were not isolated.)

3. **"Further investigation would be academic" overclaimed** given +9 MB/cycle
   under forced cleanup. Corrected: "lower priority than operational bandaid"
   rather than dismissing the investigation entirely.

4. **H6 confidence inconsistent** between "leading explanation" in grading
   section and qualifications in epistemic boundary. Reconciled: H6 is
   plausible (one border) throughout.

### Open questions from reviewers

1. ~~**Can the E2E probe run without `/api/test/cleanup`?**~~ **Answered.**
   Natural cleanup probe added and run. Results above show ~6 MB/cycle
   (natural) vs ~1 MB/cycle (forced). Natural cleanup works but retains
   ~5x more per cycle.

2. **Should the probe measure PageState / element tree counts directly?**
   Adding `gc.get_referrers()` or type-specific counts for PageState,
   pycrdt Doc, and NiceGUI Element would narrow whether the ~5 MB/cycle
   natural-cleanup gap is Python objects or allocator fragmentation.
   Not yet implemented.

## Recommendations

1. **Keep #436 threshold restart** as the primary operational mitigation.

2. **Test `MALLOC_ARENA_MAX=2`** in the systemd unit as a tuning hypothesis.
   Constrains glibc to 2 arenas instead of 32, which external guides claim
   reduces fragmentation at the cost of increased lock contention. Claimed
   tradeoffs have not been measured locally and may differ under our workload.
   Requires load testing before production deployment.

3. **Optional: add deeper object counts to diagnostics endpoint.** Track
   PageState instances, pycrdt Doc count, NiceGUI element tree size per
   client. Would narrow whether the ~5 MB/cycle natural-cleanup gap is
   Python objects (fixable) or allocator fragmentation (mitigate with
   arena tuning).

4. **In progress: isolate which `/api/test/cleanup` actions reclaim the
   ~5 MB gap.** The cleanup endpoint does three things beyond natural
   cleanup: force-delete NiceGUI clients, disconnect orphan engine.io
   sessions, and cancel orphan `Event.wait` tasks. The evidence only
   supports "the combined extra teardown reclaims ~5 MB/cycle" — it does
   not yet identify which action(s) matter. Three probe variants
   (`test_cleanup_gap_{clients,eio,events}_only`) have been added to
   isolate each action individually. Results will identify whether a
   specific fix for `on_client_delete()` is warranted and what it
   should target.

## Phase 4: Cleanup Gap Isolation Experiment

### Motivation

Prior phases established that natural cleanup retains ~6 MB/cycle while
forced cleanup retains ~1 MB/cycle. The ~5 MB/cycle gap is attributable
to the combined extra teardown in `/api/test/cleanup`, but we do not
know which of its three actions matters. Without isolation, any fix
would be guessing.

### Experimental design

**Independent variable:** Which single cleanup action runs after natural
cleanup completes.

**Dependent variable:** Per-cycle RSS growth (MB) after GC + malloc_trim.

**Controls:**
- Same workspace fixture (Pabai: 190 highlights, 181 KB CRDT, 426 KB doc)
- Same cycle count (5 cycles x 10 Playwright browsers)
- Same E2E server with 0.5s reconnect_timeout
- Natural cleanup runs first in all variants (5s wait for NiceGUI's
  Client.delete() → on_client_delete() chain to complete). The 5s budget
  covers: 0.5s reconnect_timeout + 3.0s `delete_content()` sleep + ~1.5s
  margin for 10 concurrent clients' event-loop scheduling. Phase 3 showed
  all 5 counters reach zero within this window on a clean server.
  `presence_clients` is used as the sentinel because it is the last
  counter zeroed by the cleanup chain (after dict removal, CRDT persist,
  and workspace eviction). If `presence_clients` is nonzero after the
  5s wait, natural cleanup did not complete — each variant asserts this
  on an `after_natural` snapshot taken before the targeted action runs.
- Each variant then runs exactly ONE targeted cleanup action via
  `/api/test/cleanup?mode=X`

**Three variants:**

| Variant | Mode | What it does after natural cleanup |
|---------|------|------------------------------------|
| clients_only | `clients_only` | Force-delete any remaining NiceGUI Client instances + their socket.io SIDs |
| eio_only | `eio_only` | Disconnect orphan engine.io sessions (WebSocket receive tasks surviving after client deletion) |
| events_only | `events_only` | Cancel orphan `Event.wait` tasks (leaked by NiceGUI's page handler) |

**Reference baselines (from Phase 3, fresh server):**
- Natural cleanup (no extra action): ~6 MB/cycle
- Forced cleanup (all three actions): ~1 MB/cycle

These baselines were measured on a freshly started server. The isolation
variants run on a warm server after prior tests, so absolute growth
rates may differ from Phase 3. **The primary comparison is
variant-to-variant within the isolation class**, not against Phase 3
absolute values. Phase 3 ranges are rough references only.

### Predictions (stated before running)

The predictions below use Phase 3 ranges as approximate anchors. The
diagnostic signal is which variant(s) show *substantially lower* growth
than the others, not whether any variant exactly matches Phase 3 values.

1. **If orphan engine.io sessions account for the gap:** `eio_only` will
   show substantially lower per-cycle growth than the other two variants.
   This would mean python-socketio per-session objects are the primary
   retention source. Fix: add engine.io session sweep to
   `on_client_delete()`.

2. **If Event.wait task accumulation accounts for the gap:** `events_only`
   will show substantially lower per-cycle growth than the other two.
   This would mean NiceGUI's page handler leak (documented in
   `docs/e2e-debugging.md`) is the primary retention source. Fix: cancel
   orphan Event.wait tasks in `on_client_delete()` or upstream. The
   `events_only` test asserts `orphan_wait > 0` to confirm the
   `Event.wait` qualname filter is actually matching tasks — a zero
   match would silently invalidate the variant.

3. **If stale NiceGUI clients account for the gap:** `clients_only` will
   show substantially lower per-cycle growth. This would be surprising
   since natural cleanup already zeros `Client.instances` — it would
   suggest the counter reaches zero but some client state survives.

4. **If multiple actions contribute:** Two or more variants will show
   intermediate growth (lower than natural but higher than forced).
   Individual contributions may not sum cleanly to ~5 MB due to ordering
   and interaction effects between actions.

5. **Null result (falsification):** If all three variants show growth
   comparable to each other and to natural cleanup (~6 MB/cycle), then
   either (a) the actions require each other (ordering dependency),
   (b) the gap is measurement noise amplified by execution order in the
   Phase 3 comparison, or (c) the gap is caused by something the cleanup
   endpoint does implicitly (e.g. `asyncio.sleep(0)` giving the event
   loop time to process pending callbacks).

### Execution

Run: `uv run grimoire e2e perf -k test_cleanup_gap`

Each variant runs in the same pytest session, sharing the same server
process. This means later variants inherit residue from earlier ones.
The per-cycle *growth rate* (not absolute RSS) is the metric of interest.

**Confound: cumulative server state.** If an early variant produces high
residue, later variants start from a higher RSS baseline. This could
cause growth-rate compression if arena fragmentation saturates. The
direction of bias is: later variants may appear to grow slower simply
because the allocator has fewer reclaimable pages, not because their
cleanup action is more effective. Pytest collects these tests in
alphabetical order: `clients_only` → `eio_only` → `events_only`.

**Required confirmation step.** The shared-session run identifies
candidate winner(s) but does not confirm them. Any variant that shows
substantially lower growth must be re-run in an isolated server session
to rule out saturation effects before trusting the result:
```bash
uv run grimoire e2e perf -k test_cleanup_gap_eio_only
```
If the isolated run reproduces the lower growth rate, the result is
confirmed. If not, the shared-session result was an order artefact.

**Contamination guard.** Each isolation variant takes a diagnostic
snapshot after the 5s natural wait but before the targeted cleanup
action runs (the `after_natural` phase in output). If `presence_clients`
is nonzero on any `after_natural` snapshot, the natural cleanup was
incomplete and the targeted action may have masked it — the test fails
with a contamination message.

**Positive controls.** Each variant asserts its targeted action matched
at least one target (e.g. `eio_closed > 0` for `eio_only`). A zero
match fails the test as "ambiguous." Note: for `clients_only` and
`eio_only`, a zero match is itself informative — it would mean natural
cleanup leaves no such targets to reclaim, which narrows the gap's
cause. If a positive control fails, check the cleanup response counts
before dismissing the variant.

Output files: `output/incident/e2e_probe_{clients,eio,events}_only.txt`

### Results (2026-03-28)

Full output: `output/incident/e2e_probe_{clients,eio,events}_only.txt`

Execution order was determined by pytest collection order (not
alphabetical by test name — actual order determined by class/method
declaration order or internal sorting). Order and server state inferred
from baseline RSS values:

| Order | Variant | Server state | Baseline RSS (MB) |
|-------|---------|-------------|-------------------|
| 1st | events_only | Cold (fresh server) | 283 |
| 2nd | eio_only | Warm (after 5 cycles of events_only) | 516 |
| 3rd | clients_only | Warm (after 10 total cycles) | 521 |

#### Per-cycle RSS growth (cycle-by-cycle deltas)

**events_only (cold server):**

| Cycle | after_gc RSS (MB) | Delta |
|-------|------------------|-------|
| 1 | 482 | — |
| 2 | 509 | +27 |
| 3 | 512 | +3 |
| 4 | 515 | +3 |
| 5 | 516 | +1 |

Average: 8.5 MB/cycle. But the distribution is non-linear: +27 MB in
cycle 1→2 (initial arena establishment), tapering to +1-3 MB/cycle by
cycles 3-5. The average is dominated by the first-cycle spike.

**eio_only (warm server):**

| Cycle | after_gc RSS (MB) | Delta |
|-------|------------------|-------|
| 1 | 517 | — |
| 2 | 518 | +1 |
| 3 | 520 | +2 |
| 4 | 520 | 0 |
| 5 | 521 | +1 |

Average: 1.0 MB/cycle. Growth is near-linear.

**clients_only (warm server):**

| Cycle | after_gc RSS (MB) | Delta |
|-------|------------------|-------|
| 1 | 522 | — |
| 2 | 523 | +1 |
| 3 | 524 | +1 |
| 4 | 524 | 0 |
| 5 | 525 | +1 |

Average: 0.75 MB/cycle. Growth is near-linear.

#### Isolated confirmation run (events_only on fresh server)

Per the experimental design's required confirmation step, events_only
was re-run in an isolated server session (separate pytest invocation,
fresh server process):

| Cycle | after_gc RSS (MB) | Delta |
|-------|------------------|-------|
| 1 | 480 | — |
| 2 | 504 | +24 |
| 3 | 509 | +5 |
| 4 | 514 | +5 |
| 5 | 517 | +3 |

Average: 9.25 MB/cycle. Same non-linear pattern as the shared-session
run (480→517 isolated vs 482→516 shared). The cold-start growth
pattern reproduces under isolation — it is not a shared-session
artefact.

#### Contamination guard: passed

All `after_natural` snapshots across all three variants show:
- `presence_clients` = 0
- `clients` = 0
- `ws_registry` = 0
- `tasks` = 13 (except events_only baseline which showed tasks = 12;
  a background worker started during cycle 1 and persisted)

Natural cleanup completed fully within 5s on every cycle. No targeted
action masked incomplete cleanup.

#### Positive controls: all zero

All three targeted actions matched zero targets after natural cleanup:
- `clients_only`: `deleted` = 0 across all cycles
- `eio_only`: `eio_closed` = 0 across all cycles
- `events_only`: `orphan_wait` = 0 across all cycles

**For `clients_only` and `eio_only`**, the zero match is informative:
natural cleanup leaves no stale clients or orphan engine.io sessions
to reclaim.

**For `events_only`**, the zero match requires explanation.
Investigation revealed that `_waiting_for_connection` no longer exists
in NiceGUI 3.9.0 — the Event.wait leak documented in
`docs/e2e-debugging.md` was fixed upstream. The `Event.wait` qualname
filter in the cleanup endpoint matches nothing because the underlying
bug no longer exists. This means:
- The `events_only` variant is not testing what it was designed to test
- The zero match is not evidence that "natural cleanup handles
  Event.wait tasks" — the tasks no longer exist at all
- The `events_only` variant is effectively equivalent to natural
  cleanup with no additional action

### Analysis

1. **The Phase 3 natural-vs-forced ~5 MB gap is consistent with an
   execution-order artefact (cold vs warm server).** The first variant
   to run on a cold server (events_only) shows high initial growth
   (+27 MB in cycle 1→2) that tapers to +1-3 MB/cycle by cycles 3-5.
   Later variants on the warm server show ~0.75-1 MB/cycle throughout.
   The isolated confirmation run reproduces the cold-start pattern
   (480→517 vs 482→516 in shared session), confirming it is not a
   shared-session artefact. This pattern is consistent with the Phase 3
   result where natural (cold) showed ~6 MB/cycle and forced (warm)
   showed ~1 MB/cycle.

   Evidence grade: **plausible.** The positive border is shown (cold
   server grows faster than warm). The negative border (running natural
   cleanup on a warm server shows ~1 MB/cycle, matching forced) is
   shown indirectly — eio_only and clients_only both use natural
   cleanup and show ~1 MB/cycle on the warm server. However, an
   alternative explanation cannot be fully ruled out: the warm-server
   compression could reflect allocator saturation rather than proving
   cleanup mode is irrelevant. To upgrade to demonstrated: run the
   original Phase 3 forced and natural probes with reversed execution
   order (forced first on cold server, natural second on warm).

2. **H5 (cleanup path leaves residue) is weakened but not ruled out
   under production conditions.** The `clients_only` and `eio_only`
   positive controls show natural cleanup leaves no stale clients or
   orphan engine.io sessions under E2E conditions (0.5s
   reconnect_timeout, 10 clients, no event-loop contention). Under
   production conditions (30s reconnect_timeout, 190+ clients, event
   loop under load), natural cleanup may behave differently — the
   reconnect_timeout window is 60x longer, and event-loop saturation
   can delay `Client.delete()` further. This experiment does not test
   the production path.

3. **H6 (allocator fragmentation) is consistent with the warm-server
   residue.** Warm-server variants show 0.75-1 MB/cycle after GC +
   malloc_trim. This is memory that no cleanup action can reclaim. It
   is consistent with glibc arena fragmentation. However, the negative
   border (showing that a different allocator eliminates the residue)
   has not been tested. The ~1 MB figure is an E2E measurement with
   10 clients per cycle; the per-client residue at 190 clients under
   production memory pressure is unknown.

4. **Cold-start amplification is real and reproducible.** The first
   ~2 cycles on a cold server produce disproportionate RSS growth
   (+27 MB in cycle 1→2 for events_only, +24 MB in the isolated run).
   This is not a per-cycle steady-state rate — it is a one-time cost
   for arena establishment that tapers rapidly. By cycles 3-5, the
   cold-server variant grows at +1-3 MB/cycle, approaching (but not
   matching) the warm-server rate of ~0.75-1 MB/cycle. In production,
   this means the first ~50-100 clients after a restart consume
   disproportionately more RSS than subsequent clients.

5. **The `events_only` variant is obsolete.** NiceGUI 3.9.0 removed
   `_waiting_for_connection`, fixing the Event.wait leak upstream. The
   `events_only` cleanup action matches nothing and the variant is
   effectively a pure natural cleanup test on a cold server. It should
   not be interpreted as evidence about Event.wait task behaviour.

### Updated evidence grading

| # | Hypothesis | Prior | Updated | Rationale |
|---|-----------|-------|---------|-----------|
| H1-H4 | Application-level leaks | Weakened | **Weakened** (unchanged) | All tracked counters zero under E2E conditions; deeper objects not measured; production path (30s timeout, 190 clients) untested |
| H5 | Cleanup path leaves residue | Plausible | **Weakened** — possible | Zero targets found after natural cleanup under E2E conditions (0.5s timeout). Production path (30s timeout) untested. Phase 3 gap consistent with execution-order artefact |
| H6 | glibc arena fragmentation | Plausible | **Plausible** (strengthened) | ~0.75-1 MB/cycle on warm server regardless of cleanup mode. Cold-start shows non-linear spike then taper. Negative border (allocator swap) untested |
| H7 | Sub-allocator retention | Possible | **Possible** (unchanged) | The warm-server residue could include pycrdt Yrs FFI or NiceGUI #5110 circular refs. Cannot distinguish from glibc fragmentation without allocator-level profiling |

### Epistemic boundary

- **Plausible (one border):** Warm-server per-cycle growth is
  ~0.75-1 MB regardless of cleanup mode. This is consistent with H6
  (allocator fragmentation) but the negative border — showing that
  switching allocators eliminates the residue — has not been tested.
  To upgrade to demonstrated: test with `MALLOC_ARENA_MAX=2` or
  jemalloc swap.
- **Plausible:** Natural cleanup zeroes all 5 tracked counters and
  leaves no targets for forced cleanup actions under E2E conditions
  (0.5s reconnect_timeout, 10 clients). Whether this holds under
  production conditions (30s timeout, 190+ clients, event-loop
  contention) is untested. Claiming "demonstrated" would require
  testing the production path.
- **Consistent with (not confirmed):** The Phase 3 natural-vs-forced
  gap is consistent with an execution-order artefact. The isolated
  confirmation run reproduces the cold-start pattern. But reversed
  execution order (forced-first) has not been tested, and allocator
  saturation remains an alternative explanation.
- **Obsolete:** The `events_only` variant tested for an upstream bug
  (`_waiting_for_connection` Event.wait leak) that was fixed in
  NiceGUI 3.9.0. Results from this variant should not be interpreted
  as evidence about Event.wait behaviour.
- **Not tested:** `MALLOC_ARENA_MAX=2` or jemalloc for the warm-server
  residue. `reconnect_timeout` reduction under production load.
  Production-path cleanup with 30s timeout and 190+ concurrent clients.

### Updated recommendations

1. **Keep #436 threshold restart** as the primary operational mitigation.
   With cold-start amplification (~300-350 MB for the first ~50-100
   clients) plus warm-server residue (~0.75-1 MB/client thereafter),
   a rough estimate for 200 clients/day is ~450-500 MB/day, hitting
   the 3 GB threshold in approximately 6-7 days. Faster under heavier
   load or more concurrent users.

2. **Test `MALLOC_ARENA_MAX=2`** as the next investigation step. If the
   warm-server residue is glibc arena fragmentation, constraining arena
   count should reduce it. Requires load testing before production
   deployment.

3. **Consider reducing `reconnect_timeout` from 30s to 10-15s.** Under
   E2E conditions (0.5s timeout), natural cleanup is thorough. Under
   production conditions (30s timeout), each disconnected client holds
   its full UI tree for 30s before cleanup begins. With 190+ users
   churning, this creates a significant accumulation window that E2E
   tests do not exercise. Reducing the timeout would narrow this gap.

4. **No code fix for the cleanup path is warranted under E2E conditions.**
   Natural cleanup reclaims all tracked objects and sessions within 5s
   at 0.5s reconnect_timeout. Whether this holds at 30s production
   timeout under load is untested — a production-conditions probe
   (longer timeout, more clients) would be needed to confirm.

### Peer review (Phase 4)

Reviewed by clean subagent (Opus 4.6) using critical-peer-review skill.

**High findings (3, all addressed above):**

1. "Prediction 5 confirmed" overclaimed — conclusions drawn from
   shared-session run without performing the design's required
   confirmation step. Corrected: ran isolated confirmation, downgraded
   language to "consistent with."

2. events_only ~7 MB/cycle average hid non-linear pattern (+27 MB
   spike then +1-3 MB/cycle taper). Corrected: cycle-by-cycle deltas
   reported, average contextualised.

3. Analysis violated its own protocol by drawing confirmed conclusions
   before isolated re-run. Corrected: isolated run performed, language
   downgraded throughout.

**Medium findings (4, all addressed above):**

1. "~1 MB/cycle convergence" conflated rates ranging 0.75-2.3 MB/cycle.
   Corrected: individual rates reported.

2. events_only orphan_wait=0 should have triggered investigation.
   Corrected: investigated and found `_waiting_for_connection` removed
   in NiceGUI 3.9.0 — variant is obsolete.

3. Production extrapolation used warm-server rate only. Corrected:
   blended cold+warm estimate (~450-500 MB/day, 3 GB in ~6-7 days).

4. tasks=12 vs 13 baseline difference. Noted in contamination guard
   section.

**Low findings (3):**

1. Execution order was not alphabetical. Corrected.
2. gc_collected differences not examined. Ranges overlap substantially
   (727K-799K across all variants), consistent with no difference
   between cleanup modes.
3. reconnect_timeout 0.5s vs 30s production gap. Acknowledged
   throughout as an untested production path.

Full editing pass confirmed — all downstream references to corrected
claims have been updated.

## Phase 5: Upstream Audit + MALLOC_ARENA_MAX Experiment

### Phase 5a: Upstream NiceGUI audit (2026-03-28)

Audited all three upstream NiceGUI issues against our NiceGUI 3.9.0 installation
and our ui.refreshable / ui.timer usage patterns.

#### Upstream issue status in NiceGUI 3.9.0

| Issue | Mechanism | Fix | Verified in 3.9.0 |
|-------|-----------|-----|-------------------|
| #2502 (ui.refreshable leak) | Not a framework bug — Python allocator retention | N/A | N/A |
| #5595 (ui.timer connected() leak) | `_can_start()` catches `ClientConnectionTimeout` | PR #5598 | `_waiting_for_connection` removed; `elements/timer.py:20-26` handles timeout |
| #5110 (circular refs in slot children) | `_handle_delete()` clears slot children + event listeners | PR #5644 | `element.py:518-520`: `slot.children.clear()` + `self._event_listeners.clear()` |

#### ui.timer: no leak mechanism

`ui.timer` is `nicegui.elements.timer.Timer`, extending **both** `BaseTimer` and
`Element`. When a client is deleted:

1. `Element._handle_delete()` sets `is_deleted = True`
2. `Timer._should_stop()` checks `self.is_deleted` and `self.client.id not in Client.instances` → returns True
3. Timer loop exits via `_should_stop()` check → `finally: self._cleanup()` → `self.callback = None`
4. `Timer._cleanup()` also removes itself from parent slot

Source: `.venv/lib/python3.14/site-packages/nicegui/elements/timer.py:28-40`,
`nicegui/timer.py:120-128`.

**Our timer usages assessed:**
- `pdf_export.py` polling timer: Element deletion stops it. **Not a leak.**
- `auth.py` once=True redirect timers: Fire once, cleanup runs in `finally`. **Not a leak.**
- `_search.py` / `tag_management_rows.py` debounce timers: Explicitly cancelled/deactivated. **Not a leak.**

Evidence grade: **demonstrated** (mechanism verified in NiceGUI source;
both borders shown — timer starts with client, stops when client deleted).

#### ui.refreshable: no accumulation mechanism

`refreshable` maintains `targets: list[RefreshableTarget]`. Each target contains
a `RefreshableContainer` (extends `Element`). Cleanup:

1. When client deleted, `RefreshableContainer.is_deleted` set to True via `_handle_delete()`
2. `refreshable.prune()` filters: `[t for t in self.targets if not t.container.is_deleted]`
3. `prune()` called on every `__call__()` and `refresh()` invocation

Source: `.venv/lib/python3.14/site-packages/nicegui/functions/refreshable.py:142-147`.

**Lazy pruning concern:** Stale targets persist until next `__call__`/`refresh()`.
If no client interacts with the refreshable, stale targets accumulate. However:
- The `RefreshableContainer` Element is already marked deleted (GC-eligible)
- The retention chain (target → args/kwargs) holds only the function arguments,
  not the closure state (closure belongs to `self.func`, shared across all targets)
- `RefreshableTarget` is a small dataclass (~5 fields). Not a significant
  retention source.

**Our refreshable usages assessed:**
- `tab_bar.py document_container()`: Captures PageState via closure. Prune handles
  target cleanup. PageState → refreshable → targets is a link in any retention
  chain but not a source. **Not a leak.**
- `courses.py weeks_list()`: `_course_clients` dict cleaned via `client.on_disconnect()`.
  Broadcast references properly removed. **Not a leak.**
- `header.py`, `sharing.py`, `logviewer.py`: Small captured state, low risk. **Not leaks.**

Evidence grade: **demonstrated** (mechanism verified in source; targets pruned
when container is deleted; no accumulation path in NiceGUI 3.9.0).

#### Hypothesis update from Phase 5a

| # | Hypothesis | Prior | Updated | Rationale |
|---|-----------|-------|---------|-----------|
| H8 | ui.refreshable accumulates internal state | Speculative (new) | **Not supported** | Prune mechanism removes deleted targets; RefreshableContainer cleaned by Element._handle_delete(); no accumulation path in 3.9.0 source |
| H9 | ui.timer leaks on disconnect | Speculative (new) | **Not supported** | Timer extends Element; _should_stop() checks is_deleted; _cleanup() nulls callback; #5595 fix handles connection timeout. All our usages safe |
| H6 | glibc arena fragmentation | Plausible | **Plausible** (strengthened) | With H8/H9 eliminated, H6 is the primary remaining explanation for warm-server residue |

### Phase 5b: MALLOC_ARENA_MAX Experiment (preregistration)

**Date registered:** 2026-03-28
**Status:** Preregistered, not yet run

#### Motivation

Phases 1-4 established ~0.75-1 MB/cycle (10 clients) warm-server residue that
survives forced GC + `malloc_trim(0)`. Phase 5a eliminated ui.refreshable and
ui.timer as leak sources. The leading hypothesis (H6) is glibc arena
fragmentation — and the next escalation is 1000 sessions on a 32 GB server,
where restarts are operationally costly.

glibc's `malloc` creates up to `8 * nproc` arenas by default. Each arena is a
separate memory pool. When allocations are spread across many arenas, partially-
filled arenas cannot release pages even when most allocations within them are
freed. `malloc_trim(0)` can only release pages at the *top* of the main arena;
thread-local arenas are not trimmed.

`MALLOC_ARENA_MAX` constrains the number of arenas. Fewer arenas mean:
- **Benefit:** Allocations concentrate in fewer pools → less fragmentation →
  more pages reclaimable by `malloc_trim()`
- **Cost:** Increased lock contention when multiple threads allocate concurrently
  (threads contend on arena locks instead of each getting their own arena)

**Environment comparison:**

| | Dev (this machine) | Production |
|-|-------------------|------------|
| CPUs | 32 | 4 |
| Default arenas (8 × nproc) | 256 | 32 |
| Python threads under load | ~5-10 (asyncio is single-threaded; threads from DB pool, pycrdt) | Same |

Production has 32 default arenas for ~5-10 active threads — already heavily
over-provisioned. Dev has 256 — absurdly over-provisioned. Constraining to 2
should be safe for lock contention because our thread count is low (asyncio
event loop is single-threaded; only the DB connection pool and pycrdt Yrs FFI
create threads).

#### Experimental design

**Independent variable:** `MALLOC_ARENA_MAX` setting (default / 2 / 4)

**Dependent variable:** Per-cycle RSS growth (MB) after GC + `malloc_trim()`,
measured on cycles 3-5 to exclude the worst cold-start amplification. Note:
each run starts a fresh server process, so cycles 3-5 are late-cycle
post-cold-start growth — not clean steady-state from a long-running warm
server. Phase 4's isolated fresh-server analysis showed cold-start taper
still affects cycles 3-5 (+1-3 MB/cycle) vs true warm-server (~0.75-1
MB/cycle). The comparison across arena settings is still valid (same
conditions), but absolute per-cycle values are not directly comparable to
Phase 3/4 warm-server numbers

**Test:** `test_memory_probe_434.py::TestMemoryProbe434::test_natural_cleanup_memory_growth`

Natural cleanup variant chosen because:
- It exercises the production-like path (no forced cleanup)
- Phase 4 showed natural and forced cleanup have equivalent warm-server residue
- Natural cleanup is the path that matters for production

**Controls:**
- Same workspace fixture (Pabai: 190 highlights, 181 KB CRDT, 426 KB doc)
- Same cycle count (5 cycles × 10 Playwright browsers)
- Same E2E server with 0.5s reconnect_timeout
- Each run in an isolated pytest session (separate server process) to avoid
  cross-contamination from prior test state
- Load gate remains active (no `PERF_MAX_LOAD` override). If load is too
  high, the test skips and we re-run when the machine is quieter. Each
  output file additionally records actual 1/5/15-minute loadavg for
  provenance

**How the env var propagates:** `tests/conftest.py:462-471` starts the server
subprocess with `clean_env` derived from `os.environ`, filtering only `PYTEST`
and `NICEGUI` vars. `MALLOC_ARENA_MAX` passes through unmodified.

**Execution commands:**
```bash
# Baseline (default arenas = 256 on this machine)
uv run grimoire e2e perf -k test_natural_cleanup_memory_growth

# MALLOC_ARENA_MAX=2
MALLOC_ARENA_MAX=2 uv run grimoire e2e perf -k test_natural_cleanup_memory_growth

# MALLOC_ARENA_MAX=4
MALLOC_ARENA_MAX=4 uv run grimoire e2e perf -k test_natural_cleanup_memory_growth
```

**Output files:** Each run writes to a unique timestamped file:
`output/incident/e2e_probe_natural_arena-{setting}_{ISO-datetime}.txt`

Each file includes a provenance header with timestamp, `MALLOC_ARENA_MAX`
value, `nproc`, and 1/5/15-minute loadavg at run time. No manual file
copying required — filenames are unique by timestamp and arena setting.

#### Predictions (stated before running)

**Primary prediction (H6 positive border):**

If H6 (arena fragmentation) is correct, `MALLOC_ARENA_MAX=2` will show
**substantially lower** warm-server per-cycle growth than the default. Specific
prediction: warm-server growth (cycles 3-5 average) will be ≤50% of the
default's warm-server growth.

Rationale: 256 arenas → 2 arenas is a 128x reduction. With allocations
concentrated in 2 arenas instead of scattered across 256, `malloc_trim()` has
far fewer partially-filled arenas preventing page release.

**Secondary predictions:**

1. `MALLOC_ARENA_MAX=4` will show intermediate growth between default and =2,
   but closer to =2 than to default. (4 arenas is still far fewer than 256;
   the marginal benefit of 2 vs 4 arenas is small.)

2. Cold-start amplification (cycles 1-2) may be similar across all variants.
   Arena count affects fragmentation of *freed* memory, not initial allocation.
   The first cycles allocate fresh memory regardless of arena count.

3. `gc_collected` counts will be similar across all variants. `MALLOC_ARENA_MAX`
   affects the C allocator, not Python's garbage collector. Same Python objects
   are created and freed.

4. All tracked counters (clients, presence, registry, tasks) will reach zero
   in all variants. `MALLOC_ARENA_MAX` does not affect Python-level cleanup.

**Interpretation criteria:**

- **If all three variants show the same per-cycle growth:** This **weakens**
  H6 for this E2E workload but does not falsify it outright. The workload
  uses ~5-10 threads; on this 32-core machine even the default 256 arenas
  may have most arenas unused. If threads only ever touch a few arenas,
  constraining the maximum has no effect even though fragmentation within
  those few arenas is real. A null result would shift investigation toward
  H7 (sub-allocator retention: pycrdt Yrs FFI, NiceGUI internals) or
  toward production-conditions testing where 32 default arenas (4-CPU
  server) may behave differently than 256 (32-CPU dev).

- **If `MALLOC_ARENA_MAX=2` shows HIGHER growth than default:** The experiment
  is confounded (lock contention causing allocation pattern changes) or the
  hypothesis is wrong. Stop and investigate.

- **If `MALLOC_ARENA_MAX=2` eliminates per-cycle growth entirely (0 MB/cycle):**
  Stronger than predicted. Would need to check whether `malloc_trim()` is
  now reclaiming pages that were previously trapped in per-thread arenas.
  Evidence grade would upgrade H6 to **demonstrated** (both borders: default
  shows growth, =2 eliminates it).

#### Results (2026-03-28)

Dose-response experiment: default, =2, =3, =4, =5 arenas. Each run in an
isolated pytest session (fresh server process). Load gate active (no bypass).

**Contaminated runs (discarded):**
- =3 first run: loadavg 48.94 — discarded, re-run at 2.13
- =4 first two runs: loadavg 34.70 and 110.87 — discarded, re-run at 5.60

**Clean runs (loadavg <6):**

| Setting | loadavg (1m) | Output file | Cycle deltas | Late-cycle avg (3-5) |
|---------|-------------|-------------|-------------|---------------------|
| default (256) | 3.57 | `arena-default_20260328T071530Z` | +26.6, +3.3, +5.8, +1.0 | **+3.4** |
| =5 | 2.86 | `arena-5_20260328T083412Z` | +31.3, +3.7, +1.5, +1.5 | **+2.2** |
| =4 | 5.60 | `arena-4_20260328T085601Z` | +20.0, +10.3, +2.2, +0.2 | **+4.2** |
| =3 | 2.13 | `arena-3_20260328T084045Z` | +27.3, +3.3, +4.5, +2.8 | **+3.5** |
| =2 | 3.05 | `arena-2_20260328T073154Z` | +28.6, +4.5, +3.0, **-2.9** | **+1.6** |

Ordered by late-cycle average growth:

```
=2:       +1.6 MB/cycle  (goes NEGATIVE by cycle 5: -2.9 MB)
=5:       +2.2 MB/cycle
default:  +3.4 MB/cycle
=3:       +3.5 MB/cycle
=4:       +4.2 MB/cycle  (5-min loadavg still elevated at 37.40)
```

#### Analysis

1. **`MALLOC_ARENA_MAX=2` is the clear winner.** It is the only setting
   where `malloc_trim()` actually *reclaims* memory (cycle 5: -2.9 MB).
   Late-cycle average is 1.6 MB/cycle vs 3.4 for default — 47% of
   default, matching the preregistered ≤50% prediction.

2. **=3 through =5 cluster around the default** (+2.2 to +4.2 vs +3.4).
   The signal is weak and noisy in this range. This is consistent with
   the preregistration's caution: with ~5-10 active threads on a
   256-arena (32-CPU) machine, most arenas are already unused.
   Constraining to 3-5 has little effect — the jump from =3 to =2 is
   where consolidation crosses a threshold.

3. **Cold-start amplification is consistent across all settings.** Cycle
   1→2 is always +20-31 MB regardless of arena count. This confirms the
   preregistered prediction that arena count affects fragmentation of
   *freed* memory, not initial allocation patterns.

4. **gc_collected is consistent across all settings** (~727K-818K range).
   As predicted, `MALLOC_ARENA_MAX` affects the C allocator only, not
   Python's garbage collector.

5. **All tracked counters reach zero in all runs.** Clients, presence,
   registry, tasks — all clean across all arena settings.

#### Prediction check

| Prediction | Result |
|-----------|--------|
| =2 late-cycle ≤50% of default | 1.6/3.4 = 47%. **Met.** |
| =4 intermediate between =2 and default | 4.2 > 3.4 (default). **Not met** — but =4 run had elevated 5m loadavg. Noisy. |
| Cold-start similar across variants | All +20-31 MB in cycle 1→2. **Met.** |
| gc_collected similar | 727K-818K range. **Met.** |
| Counters reach zero in all | Yes. **Met.** |
| Null result weakens H6 | N/A — positive result observed for =2. |

#### Evidence grading update

| # | Hypothesis | Prior | Updated | Rationale |
|---|-----------|-------|---------|-----------|
| H6 | glibc arena fragmentation | Plausible | **Plausible (strengthened)** | =2 shows negative cycle-5 delta (trim reclaims memory); late-cycle avg halved. Positive border shown. Negative border (showing default *cannot* reclaim) shown indirectly — default never goes negative. However, the dose-response curve is noisy for =3-5, and the dev machine (32 CPUs, 256 default arenas) does not match production (16 CPUs, 128 default arenas). To upgrade to demonstrated: test =2 on production under real load. |

#### Production deployment considerations

**Target environment:** DigitalOcean, 16 CPUs, 32 GB RAM.
Default `MALLOC_ARENA_MAX` = 8 × 16 = 128 arenas.

**Recommendation: Deploy `MALLOC_ARENA_MAX=2` to production.**

The dev machine result (256 default arenas, =2 halves residue and enables
trim reclamation) should transfer to production (128 default arenas) because:
- The mechanism is the same: fewer arenas → more consolidation → more
  reclaimable pages
- 128 default arenas is still massively over-provisioned for ~5-10 threads
- The jump to =2 is where the effect appears (=3-5 cluster around default)

**Implementation:**

Add to the systemd unit override:
```ini
[Service]
Environment=MALLOC_ARENA_MAX=2
```

**Risk assessment:**
- Lock contention: Low risk. ~5-10 active threads (asyncio is single-
  threaded; threads from DB pool + pycrdt Yrs FFI). 2 arenas is still
  more than 1, so concurrent allocators can avoid each other.
- Latency impact: Possible but unlikely. No application-level latency
  monitoring currently exists (see observability gaps below).
- Worst case: Remove the env var, restart. No code changes. Instant
  rollback.

**Observability: what we have vs what the rollback plan needs.**

| Signal | Have it? | Source | Notes |
|--------|----------|--------|-------|
| RSS over time | **Yes** | `memory_diagnostic` every 5 min (`diagnostics.py:207`), Beszel system memory | Beszel monitors system-level memory, not rate-of-change. Diagnostic logger provides per-snapshot RSS. |
| #436 restart frequency | **Yes** | systemd journal (`exit code 75`) | Each threshold restart is logged. More restarts = worse. |
| Client/task counts | **Yes** | `memory_diagnostic` event fields | Tracked counters in each snapshot. |
| p99 response latency | **No** | — | `/healthz` returns `"ok"` unconditionally; UptimeRobot is a 5-min uptime check, not a latency monitor. No application-level latency metrics exist. |
| Event loop iteration time | **No** | — | `collect_snapshot()` does not measure event-loop lag. |

**Defensible rollback triggers (using signals we actually have):**

1. **RSS slope got worse.** Compare `memory_diagnostic` RSS values over
   first 24h with =2 against the pre-change baseline from the same
   server. Specific query:
   ```sql
   SELECT ts_utc, json_extract(extra_json, '$.rss_bytes') / 1048576.0 AS rss_mb
   FROM jsonl_events
   WHERE event = 'memory_diagnostic'
   ORDER BY ts_utc
   ```
   If the slope (MB/hour) is steeper than baseline, revert.

2. **#436 restart frequency increased.** Check systemd journal for
   `exit code 75` restarts. If restarts happen more often than before
   the change, revert.

3. **User reports of sluggishness.** Without latency monitoring, user
   complaints are the only latency signal. This is a gap.

**Observability gap: latency monitoring is not in scope for this PR**
but should be addressed separately. Adding event-loop lag measurement
to the diagnostic logger and/or instrumenting `/healthz` with a DB
ping + response time would provide the missing rollback signal for
future tuning experiments.
